// Compatibility import for the first standalone reconstruction draft.
// Boundary-layer geometry now has one validated implementation.
export { reconstructDeterministicImageBoundary } from './boundaryLayers';
